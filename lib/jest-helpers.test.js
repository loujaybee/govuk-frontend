/* eslint-env jest */

const {
  toHaveNoViolations,
  runAccessibilityTests
} = require('./jest-helpers')

describe('Jest helpers', () => {
  describe('accessibility', () => {
    it('default example passes accessibility tests', () => {

    })
  })
})
